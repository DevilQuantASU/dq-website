const guide = {
  slug: "sma-trading-bot",
  title: "Simple Moving Average Bot",
  description: "Build your first algorithmic trading script using a classic momentum indicator: the Simple Moving Average (SMA).",
  sections: [
    {
      id: "project-overview",
      title: "Project Overview",
      content: `
        <p>The Simple Moving Average (SMA) crossover is one of the most fundamental trading strategies. In this project, you'll learn how to compute moving averages, generate trading signals, and backtest the strategy's performance using Python.</p>
        <p><strong>Goal:</strong> Implement a strategy that buys a stock when its short-term moving average crosses above its long-term moving average, and sells when it crosses below.</p>
      `,
    },
    {
      id: "the-strategy",
      title: "The Strategy Explained",
      content: `
        <p>Moving averages smooth out price data by creating a constantly updated average price. We use two of them:</p>
        <ul>
          <li><strong>Short SMA (e.g., 20-day):</strong> Reacts quickly to recent price changes.</li>
          <li><strong>Long SMA (e.g., 50-day):</strong> Represents the longer-term trend.</li>
        </ul>
        
        <div class="guide-callout guide-callout-info">
          <strong>The Golden Cross (Buy Signal):</strong> When the Short SMA crosses <em>above</em> the Long SMA. This indicates upward momentum.<br/><br/>
          <strong>The Death Cross (Sell Signal):</strong> When the Short SMA crosses <em>below</em> the Long SMA. This indicates downward momentum.
        </div>
      `,
    },
    {
      id: "calculating-smas",
      title: "Calculating SMAs with Pandas",
      content: `
        <p>Assuming you already have a Pandas DataFrame of historical prices (e.g., using <code>yfinance</code>), calculating SMAs is very simple using the <code>rolling()</code> method.</p>
        
        <pre><code class="language-python"># Assuming 'df' is your DataFrame and has a 'Close' column
import pandas as pd

# Calculate the 20-day and 50-day moving averages
df['SMA20'] = df['Close'].rolling(window=20).mean()
df['SMA50'] = df['Close'].rolling(window=50).mean()

# Drop rows with NaN values generated by the rolling window
df.dropna(inplace=True)
</code></pre>
      `,
    },
    {
      id: "generating-signals",
      title: "Generating Buy/Sell Signals",
      content: `
        <p>Next, we need to create a new column that holds our trading signal. We'll use <code>1</code> for a Buy signal and <code>-1</code> for a Sell signal.</p>
        
        <pre><code class="language-python">import numpy as np

# Create a 'Signal' column: 1 if SMA20 > SMA50, else -1
df['Signal'] = np.where(df['SMA20'] > df['SMA50'], 1, -1)

# Calculate daily market returns
df['Market_Return'] = df['Close'].pct_change()

# Calculate our strategy returns (Shift the signal by 1 day to prevent look-ahead bias!)
df['Strategy_Return'] = df['Signal'].shift(1) * df['Market_Return']
</code></pre>
        
        <div class="guide-callout guide-callout-warning">
          <strong>Crucial Step:</strong> Notice the <code>shift(1)</code>. If you generate a signal based on today's close, you can only trade on tomorrow's open/close! Forgetting to shift your signals is the #1 cause of unrealistically profitable backtests.
        </div>
      `,
    },
    {
      id: "resume-extensions",
      title: "Making it Resume-Worthy",
      content: `
        <p>To turn this basic script into an impressive project, consider adding these features:</p>
        
        <div class="guide-callout guide-callout-good">
          <strong>Project Enhancements:</strong>
          <ul>
            <li><strong>Parameter Optimization:</strong> Write a loop to test different SMA combinations (e.g., 10/30 vs 20/50) to see which performed best historically.</li>
            <li><strong>Plot the Strategy:</strong> Use <code>matplotlib</code> to plot the stock price, the two SMA lines, and scatter plot markers for the Buy/Sell executions.</li>
            <li><strong>Calculate Metrics:</strong> Compute the Sharpe Ratio or the Maximum Drawdown of your strategy compared to simply holding the stock (Buy and Hold).</li>
          </ul>
        </div>
      `,
    }
  ],
};

export default guide;
